import { loadHeaderFooter } from "./utils.mjs";


// Call the function to load header and footer when the DOM content is loaded
document.addEventListener('DOMContentLoaded', () => {
    loadHeaderFooter();
    // Also call renderCartContents here if needed
  });

